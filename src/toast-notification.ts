import { XToastNotification } from "./components/toast-notification";
import { defineOnce } from "./components/utils";

defineOnce(XToastNotification.tagName, XToastNotification);

export { XToastNotification };
